# URL base do backend (sem barra no final)
# Em dev não precisa definir — o proxy do Vite cuida disso
# Em produção (Vercel), aponte para o serviço do Render:
VITE_API_URL=https://cleansistem-api.onrender.com
